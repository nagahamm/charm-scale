# Project Guidelines: charm-scale

マッチングアプリの会話スクショ・プロフィール写真を Claude に読ませ、食いつき度数 / メッセージ添削 / 写真評価を返す振り返りツール。

## Communication Rules

- 敬語・挨拶・前置き・後置きは不要。
- 結論、変更内容、コードのみを最小限の言葉で伝える。

## Interaction & Execution Policy

- **Issueを起点に開発を進める。** 着手前にIssueを立て、症状・原因・対応方針を残してから実装する。バグ修正・機能追加・調査のいずれも対象。typoやフォーマット修正など、説明の必要がない些細な変更は除く。
- **作業中は確認しない。** 修正・テスト・コミットまでを一括で自律的に実行し切る。
- **質問してよいのは2つの場合のみ。** 着手前の前提確認と、予期せぬ破壊的変更のリスクがあり判断不能な場合。
- **`/compact` と `/clear` を活用する。** 細かい作業ごとではなく、文脈の異なる作業が5回程度重なった時点、または無関係なコンテキストが溜まった時点でリセットする。

## 開発手法

プロジェクトの規模が大きくなったため、思いつきで実装を始めない。以下の順序を守る。

- **要件定義(BDD寄り)**: 機能に関わる変更は、着手前に [`docs/requirements.md`](docs/requirements.md) に Gherkin 形式(Given/When/Then)のシナリオを追記・更新する。ステークホルダー(利用者本人)が読んで齟齬に気づけることを優先し、実装用語ではなく振る舞いで書く。
- **詳細設計(DDD寄り)**: ドメインモデル・DB設計に関わる変更は、[`docs/design.md`](docs/design.md) を先に更新する。業務上の関心事(User/Person/Analysis 等)を中心に据え、実装の都合でモデルを歪めない。
- **実装(TDD寄り)**: 検証可能な範囲(構文チェック・入力バリデーション・モデルのパース・`flutter test`)はテストを用意し、通してからコミットする(既存の「開発環境の制約」節と同じ原則)。すべてを先にテストから書く厳密なTDDまでは求めないが、テストなしでの実装のみのコミットはしない。
- **AIDD**: 本プロジェクトの実装は Claude Code が要件定義からコード・テストまで一貫して担う。上記の手順(要件定義 → 設計 → 実装)を飛ばして直接コードから着手しない。
- Issue はこれらの成果物(要件定義書のシナリオ・設計書の該当箇所)への言及を含めてよいが、要件定義書・設計書そのものの代わりにはしない。Issueは「何をなぜやるか」の記録、`docs/` は「現在どうなっているか」の生きたドキュメント。

## モジュール責務

メインは Flutter による iOS / Android アプリ(`app/`)。Web 版は廃止し、バックエンドは API 専用エンドポイントとして残す。

| 領域 | ファイル | 責務 |
| :--- | :--- | :--- |
| 画面 | `app/lib/screens/` | 画面構成・状態管理・ナビゲーション |
| 部品 | `app/lib/widgets/` | 画面から再利用される表示部品。ロジックを持たない |
| デザイントークン | `app/lib/theme.dart` | 色・サイズを一元管理。ウィジェットにリテラル値を直書きしない |
| モデル | `app/lib/models/` | API レスポンスの型定義とパース。スキーマの定義自体は持たない |
| 通信 | `app/lib/services/` | 画像の取り込み・縮小(`image_prep.dart`)、NDJSON ストリーム受信(`analysis_api.dart`)。APIキーを持たない |
| API | `api/functions/analyse.mjs` | Claude API 呼び出し、入力バリデーション、プロンプトとJSONスキーマの定義、解析結果の永続化(書き込み専用。Rawログ保存 + Zod検証 + 正規化DB保存、[design.md](docs/design.md) 1.1節) |
| API | `api/functions/analyses.mjs` | Person(相手)のCRUDと、Personに紐づくAnalysis履歴の一覧・詳細取得(読み出し専用。正規化テーブルからCHAT_SCHEMA形式のJSONを再構成する、[design.md](docs/design.md) 4.2節) |
| API | `api/functions/signup.mjs` | 新規登録の受付可否の確認(読み出し専用・認証不要。`signup_accepting()` の結果を返すだけ、[design.md](docs/design.md) 4.1節) |
| API(共通) | `api/functions/persistence.mjs` | Supabaseクライアント初期化・JWT検証・Zodスキーマ・利用回数の集計(1日あたりの解析回数上限、[design.md](docs/design.md) 4.4節)を集約。他のAPIファイルはここ経由でSupabaseにアクセスする |

- プロンプトとJSONスキーマ(Claude用)は `analyse.mjs` に集約する。アプリ側に散らさない。
- APIキーは環境変数 `ANTHROPIC_API_KEY` のみから読む。アプリのビルドに埋め込まない。Supabase の Service Role キーも同様にサーバー側の環境変数のみから読み、アプリには渡さない。Service Role キーを直接 `process.env` から読むのは `persistence.mjs` だけにする。
- 認証(サインアップ/ログイン)は Flutter から Supabase Auth に直接行う。DBの読み書き(解析結果の保存・取得)は `analyse.mjs` を経由する([design.md](docs/design.md) 1節)。

## Core Principles

- **SOLID**: 各モジュールの責務を上表の通りに保つ。画面(UI)/部品/モデル/通信/API の実装をまたいで混ぜない。
- **KISS**: 素直に書く。動く最小の実装を選ぶ。Flutter 標準(`StatefulWidget`/`setState`)の範囲で完結させ、状態管理ライブラリやコード生成ツールを追加しない。
- **YAGNI**: 今必要でない設定項目・抽象化・オプションを足さない。
- **DRY**: 同じロジック・同じ定数を二度書かない。既存の関数を再利用できるならそちらを使う。

## Git Commit Rules

Conventional Commits 規約(英語)に準拠する。

```
<type>(<scope>): <short summary>

- 変更点を箇条書き

Fixes #<issue番号>
```

対応するIssueを `Fixes #N`(バグ修正)または `Closes #N`(機能追加)で必ず閉じる。1つのIssueに複数コミットが紐づく場合は、最後のコミットにのみ書く。

### 粒度はタスク単位で決める

- **1コミット = 1タスク。** 「なぜこの変更をしたか」が1つの理由で説明しきれる単位に分割する。複数の目的を混ぜない。
- 性質の異なる変更(バグ修正 / リファクタリング / 新機能 / パフォーマンス / ドキュメント)は、同じファイルを触る場合でも必ず分ける。
- 同種のタスクや機械的に連動する変更(例: クラス名リネームに伴う複数ファイルの同時修正)は1コミットにまとめてよい。ファイル単位ではなくタスク単位で判断する。

### 順序と状態

- **各コミットは単体で成立させる。** どの時点でチェックアウトしても動作し、構文エラーが無い状態を保つ。「次のコミットで直る」中間状態を残さない。
- **バグ修正が先、リファクタリングが後。** 原因と対処がリファクタリングの巨大な差分に埋もれないようにする。
- **大きな作業は着手前に分割方針を決める。** 作業後にまとめてコミットして後から分割し直す手戻りを防ぐ。

### `git commit --amend`

直前のコミットの修整・補足・関連作業であれば `--amend` で統合する。新規作業の着手時にこれに該当する場合は、作業前にその旨を提案する。別タスクであれば必ず新規コミットにする。

## タグ

バグ修正・機能追加が `main` にマージされた時点で、`main` 上にタグを打つ。`vMAJOR.MINOR.PATCH` を使う。

**一般公開前(開発中)は `v0.MINOR.PATCH` を使う。** `MAJOR` を 1 に上げるのは実際に一般公開したタイミングのみ。開発中に「全面刷新」規模の変更があっても、`v0.x.0` として MINOR を上げるにとどめる(例: `v0.4.0` → `v0.5.0`)。一般公開後は下記のSemVer寄りの運用に切り替える。

コミットの `type` ではなく、**利用者から見た変化の大きさ**で決める。ライブラリ的な厳密なSemVerではなく、一般的なアプリのリリース慣習に合わせる。`fix` が1件でも `feat` が混ざっていても、体感が変わらない範囲なら PATCH のままでよい。

- **PATCH**: バグ修正、内部改善、パフォーマンス改善、既存の挙動を安定させるための調整。使い方が変わらないものは全てここ。日常的なリリースはほぼこれになる。
- **MINOR**: 利用者が「新しくできるようになった」と分かる機能の追加、UIの変更。開発中(`v0.x.x`)はここが実質のメイン桁。
- **MAJOR**: 全面刷新、または旧環境がそのままでは動かなくなる非互換変更(環境変数やレスポンス形式の変更など)。ただし一般公開前は 0 のまま据え置く。
- 1コミット1タグにしない。`main` へマージした単位でまとめて1つのリリースとする。
- `docs` / `refactor` のみの変更ではタグを打たない。
- 利用者側の再設定が必要な場合(Netlify の環境変数追加など)は、バージョンを上げる理由にはしないが、タグの注釈に必ず手順を明記する。
- 注釈付きタグで、含まれる変更とIssue番号を残す。

```
git tag -a v0.6.0 -m "Release v0.6.0: <利用者から見た変更点> (#1, #2, #3)"
git push origin v0.6.0
```

## 開発環境の制約

- バックエンドは Node.js + Netlify Functions。ローカルは `npm install` → `npx netlify dev`(http://localhost:8888)。
- アプリは Flutter。ローカルは `app/` で `flutter pub get` → `flutter run --dart-define=API_BASE_URL=<バックエンドのURL>`。
- **Claude API を実際に叩く経路は `ANTHROPIC_API_KEY` が無いと検証できない。** キーが無い環境では、動作確認できない箇所を推測で「確認済み」と書かず、キーを入れた環境での確認を促すこと。
- キー無しでも検証できるもの: 構文チェック(`node --check`)、入力バリデーション(メソッド不正・画像0枚・非対応形式)、ストリーム配線、エラーメッセージの変換、Flutter側のモデルパース(`flutter test`)、静的解析(`flutter analyze`)。**検証可能なものは検証してからコミットする。**
- 実機・シミュレーターでの目視確認が必要な変更(レイアウト・画面遷移)は、自動テストを実行せず、確認箇所を明示して人に委ねる。

## Typography & Formatting Rules

1. **ダッシュ記号**: 区切りには `—`(エムダッシュ, U+2014)を使用する。
2. **強調の引用符**: 日本語本文中の強調には `「」` を使用し、`""` は使用しない。
3. **三点リーダー**: 半角ピリオド連続(`...`)ではなく `…`(U+2026)を使用する。
4. **英語表記**: カラーに関する単語はオーストラリア/イギリス英語(`colour`)を使用する。

## Constraints

- **Privacy**: スクショには第三者の個人情報が写る。画像をサーバーやログに保存する実装を追加しない。
- **Safety**: 相手を操作・強要する助言を返すプロンプトを書かない。改善対象は常に利用者自身の振る舞い。容姿そのものへの侮辱、年齢・人種・健康状態の推定を評価材料にしない。
- **Model**: `claude-opus-5` を使う。モデルIDに日付サフィックスを付けない。出力は Structured Outputs(`output_config.format`)でスキーマに固定する。
